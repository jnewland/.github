{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "default package rules from https://github.com/jnewland/.github/blob/main/default.json",
  "extends": [
    "config:base",
    ":disableDependencyDashboard",
    ":maintainLockFilesWeekly",
    ":semanticCommits",
    ":semanticCommitScope(deps)",
    "docker:enableMajor",
    "docker:pinDigests",
    "group:linters",
    "helpers:pinGitHubActionDigests"
  ],
  "labels": [
    "renovate",
    "{{datasource}}"
  ],
  "rebaseWhen": "conflicted",
  "prFooter": "",
  "prHeader": "Greetings. Auto-update behavior is configured via `renovate.json` in this repo, which inherits defaults from https://github.com/jnewland/.github.",
  "packageRules": [
    {
      "updateTypes": [
        "major"
      ],
      "addLabels": [
        "major"
      ]
    },
    {
      "updateTypes": [
        "minor"
      ],
      "addLabels": [
        "minor"
      ]
    },
    {
      "updateTypes": [
        "patch"
      ],
      "addLabels": [
        "patch"
      ]
    },
    {
      "description": "Update the Renovate GitHub action weekly",
      "packageNames": "renovatebot/github-action",
      "extends": [":semanticCommitType(fix)", "schedule:weekly"]
    }
  ],
  "regexManagers": [
    {
      "fileMatch": [
        "(deployments|daemonsets)/.+\\.yaml$"
      ],
      "datasourceTemplate": "github-releases",
      "matchStrings": [
        "(ghcr|quay)\\.io\\/(?<depName>.+):(?<currentValue>.+)"
      ]
    },
    {
      "fileMatch": [
        ".+"
      ],
      "datasourceTemplate": "github-releases",
      "matchStrings": [
        "https://raw\\.githubusercontent\\.com/(?<depName>[^/]+/[^/]+)/(?<currentValue>[^/]+)/"
      ]
    },
    {
      "fileMatch": [
        ".+"
      ],
      "datasourceTemplate": "github-releases",
      "matchStrings": [
        "https://github\\.com/(?<depName>[^/]+/[^/]+)/releases/download/(?<currentValue>[^/]+)/"
      ]
    },
    {
      "fileMatch": [
        ".+"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?)( versioning=(?<versioning>.*?))?\\s.*_?(version|VERSION)(:|=) ?(?<currentValue>.*)\\s"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver{{/if}}"
    }
  ]
}
